import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Platform Insights',
  description: 'Compare platform performance and get personalized recommendations to maximize your gig worker earnings',
};

export default function InsightsPage() {
  return null; // Dashboard component handles rendering
}
